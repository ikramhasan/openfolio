/**
 * The class-name merger the generated editor components import. shadcn's newer
 * base components import `cn` from the package directly; the Plate components
 * expect it behind the `utils` alias, so this is the one place it is named.
 */
export { cn } from "cn";
